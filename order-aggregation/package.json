{
  "name": "order-aggregation",
  "version": "0.0.1",
  "description": "Aggregate the orders back together, grouping them by item_id",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "docker build -t order-aggregation:0.0.1 .",
    "start": "node main.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@axway/ace-sdk": "^0.0.8",
    "bytebuffer": "^5.0.1"
  },
  "devDependencies": {
    "@types/bytebuffer": "^5.0.40",
    "@types/long": "^4.0.0"
  }
}
